# Anthropic (Claude) API キー。設定するとAIによるアドバイス生成が有効になります。
# 未設定の場合は、関連記事の一覧表示のみ行うフォールバックモードで動作します。
ANTHROPIC_API_KEY=

# 使用するモデル（省略時は claude-sonnet-5）
ANTHROPIC_MODEL=claude-sonnet-5

# サーバーのポート番号（省略時は 3000）
PORT=3000
